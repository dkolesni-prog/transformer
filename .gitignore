# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
.DS_Store
.golangci.yml
Dockerfile
cmd/shortener/shortener_data.json
*shortener_data.json*
ZAP*
allProjectLinters.txt
ci.txt
cmd/shortener/task.txt
golangci-lint/
internal/app/test_data.json
shortener
shortener_data.json
cmd/shortener/shortener_data.json
.env
docker-compose.yml


golangci-lint/.cache/

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Binaries
cmd/shortener/main
cmd/shortener/shortener

# Dependency directories (remove the comment below to include it)
vendor/

# IDEs directories
.idea
.vscode
